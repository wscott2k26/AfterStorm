# AfterStorm World Polish + Azure CI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Move everyday CI from GitHub Actions to Azure Pipelines and upgrade The Block into a closer, more alive premium restoration diorama with staged lights, nature, weather, residents, and stronger restoration payoff.

**Architecture:** Keep GitHub as the source of truth. Add an Azure Pipelines YAML that runs core tests, deterministic asset generation, XcodeGen, an iOS Simulator build, runtime smoke launch, and acceptance screenshots on Microsoft-hosted macOS. Change GitHub Actions to manual-only for the expensive Xcode 27 smoke/release lane. For visuals, keep `WorldDioramaView` as the rendering boundary and derive environment changes from the tested core restoration/weather state so every quest leaves a visible mark without adding new persistence models.

**Tech Stack:** SwiftUI, RealityKit, AfterStormCore, XcodeGen, Swift Package Manager, Azure Pipelines YAML, GitHub Actions.

## Global Constraints

- App baseline remains iOS 18.
- iOS 27 multimodal Foundation Models stays availability-gated and nonessential to core functionality.
- No new third-party runtime dependencies.
- No destructive streak or regression mechanics.
- Every quest completion must produce visible world feedback.
- GitHub Actions heavy macOS jobs must no longer run automatically on pushes/PRs.
- Azure is the normal development CI lane; GitHub Actions is a manual/release verification lane.
- Preserve Reduce Motion behavior and accessibility labels.

---

### Task 1: Azure Pipelines development CI

**Files:**
- Create: `azure-pipelines.yml`
- Modify: `.github/workflows/ios-ci.yml`
- Create: `docs/quality/azure-pipelines-setup.md`

**Interfaces:**
- Consumes: `scripts/generate-assets.py`, `project.yml`, `Package.swift`, `AfterStorm.xcodeproj` generated by XcodeGen.
- Produces: an Azure pipeline that runs tests, builds the app/widget, boots a simulator and publishes acceptance screenshots.

- [x] Add `azure-pipelines.yml` using `macOS-26`, select the newest installed Xcode 26.x path dynamically, run `swift test`, install XcodeGen, generate the project, build with `CODE_SIGNING_ALLOWED=NO`, and reject `warning:` output.
- [x] Change GitHub Actions triggers to `workflow_dispatch` only so ordinary pushes and PRs consume zero GitHub-hosted macOS minutes.
- [x] Document the exact Azure DevOps UI steps to create the AfterStorm pipeline from the existing GitHub repo and explain that Microsoft-hosted free parallelism must be enabled on the DevOps organization.
- [x] Add simulator install/launch, runtime crash screening and deterministic screenshot artifacts to Azure CI.
- [x] Verify YAML structure and referenced repository paths locally.

### Task 2: Closer premium world composition

**Files:**
- Modify: `AfterStormApp/World/WorldDioramaView.swift`
- Modify: `AfterStormApp/World/WorldHomeView.swift`

- [x] Increase the visual footprint of The Block, move it into the hero area, and reduce the oversized empty storm-sky area.
- [x] Add layered street/ground depth, foreground haze and wet-road reflection bands without new runtime image dependencies.
- [x] Make the World control panel more compact while preserving Give Me a Quest, Scan, Tell, Map and Residents.
- [x] Preserve safe-area behavior, Reduce Motion and accessibility.

### Task 3: Restoration-based lighting system

**Files:**
- Modify: `AfterStormApp/World/WorldDioramaView.swift`

- [x] Add streetlamp silhouettes with deterministic stage activation thresholds.
- [x] Add corner-store/sign glow and door/window warmth that grows with restoration.
- [x] Add reflected light under restored lamps/buildings so lighting changes affect the scene, not just isolated icons.
- [x] Keep all thresholds deterministic from restoration stage count.

### Task 4: Nature restoration and weather evolution

**Files:**
- Modify: `AfterStormApp/World/WorldDioramaView.swift`
- Modify: `AfterStormApp/Design/AfterStormTheme.swift`

- [x] Reuse the tested core `WorldWeatherState` so app and widget share one Stormy → Clearing → Afterglow definition.
- [x] Adjust sky gradient, cloud glow, rain density, ambient brightness and afterglow by weather state.
- [x] Add tree trunks/canopies whose canopy size/saturation increases with progress.
- [x] Add flower/nature accents at mid/high restoration.
- [x] Keep Reduce Motion users on static equivalents.

### Task 5: Stormling/resident life

**Files:**
- Modify: `AfterStormApp/World/WorldDioramaView.swift`

- [x] Replace generic floating life markers with grounded miniature resident/Stormling silhouettes.
- [x] Add multiple distinct Stormling/human positions plus animal and bird life cues.
- [x] Animate only subtle idle offsets when Reduce Motion is off.
- [x] Hide/simplify motion with Reduce Motion.

### Task 6: Stronger restoration reveal

**Files:**
- Modify: `AfterStormApp/World/RestorationRevealView.swift`
- Modify: `AfterStormApp/World/WorldDioramaView.swift`

- [x] Add a controlled diorama push-in/settle animation.
- [x] Add a Sparks/afterglow restoration pulse rather than a generic white flash.
- [x] Keep existing audio/haptic reward and reaction copy.
- [x] Ensure Reduce Motion switches to opacity/brightness-only behavior.

### Task 7: Verification and release evidence

**Files:**
- Modify: `scripts/verify-local.sh`
- Modify: `docs/quality/v1-acceptance.md`
- Modify: `README.md`

- [x] Run the platform-neutral core suite on the current premium-World code mirror: **22 passed, 0 failed**.
- [x] Parse the native Swift source and validate CI/project YAML; the exact changed files were hash-matched to GitHub before verification.
- [x] Scan for forbidden release markers and unsafe `try!` / `as!` usage.
- [x] Preserve the last green Xcode 27 simulator baseline and explicitly mark the new visual head as awaiting its first Azure/macOS compile + screenshots rather than spending additional GitHub macOS minutes.
- [x] Update acceptance documentation and README with Azure migration and visual-polish scope.
- [x] Keep `main` untouched until the new premium branch receives a green Azure macOS build and visual review.

## Implementation status

**Repository-side implementation complete.** The remaining action is the one-time Azure DevOps organization/project authorization and first pipeline run. That account-level action cannot be performed through the currently connected tools because no Azure DevOps connector is installed. Once the user connects the existing GitHub repo to `/azure-pipelines.yml`, Azure will produce the fresh macOS build result and new simulator screenshots required for the merge gate.
